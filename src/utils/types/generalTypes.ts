export interface TableInfo {
    id?: number,
    area?: string,
    number?: string,
    status?: string
}

export interface DateInfo {
    year: number,
    month: number,
    day: number
}

export interface TimeInfo {
    hour: number,
    minute: number
}
